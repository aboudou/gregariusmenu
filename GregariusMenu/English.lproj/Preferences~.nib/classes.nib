{
    IBClasses = (
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            ACTIONS = {myAction = id; }; 
            CLASS = NSButton; 
            LANGUAGE = ObjC; 
            SUPERCLASS = NSControl; 
        }, 
        {
            ACTIONS = {saveChanges = id; }; 
            CLASS = PreferencesController; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                autosnoozeButton = NSButton; 
                beepNotificationButton = NSButton; 
                gregariusURLField = NSTextField; 
                growlNotificationButton = NSButton; 
                newGregariusRPCButton = NSButton; 
                nixonModeButton = NSButton; 
                pollRateField = NSTextField; 
                snoozeTimeField = NSTextField; 
                startAtLoginButton = NSButton; 
            }; 
            SUPERCLASS = NSWindowController; 
        }
    ); 
    IBVersion = 1; 
}